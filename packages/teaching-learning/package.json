{
  "name": "@knowget/teaching-learning",
  "version": "0.1.0",
  "private": true,
  "description": "Teaching, Learning & Instruction Intelligence Platform (P2-D09) — the authoritative domain for planning, delivering, monitoring and continuously improving instruction: academic plans, unit plans, version-controlled lesson plans, learning resources, classroom sessions (planned vs actual delivery), assignments and learning evidence, with a pure instructional-intelligence engine. Every instructional activity is traceable to curriculum outcomes.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@knowget/shared": "workspace:*",
    "@knowget/types": "workspace:*",
    "@knowget/exceptions": "workspace:*",
    "@knowget/events": "workspace:*"
  },
  "devDependencies": {
    "@knowget/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
