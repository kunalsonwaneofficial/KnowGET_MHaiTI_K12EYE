{
  "name": "@knowget/learner-wellbeing",
  "version": "0.1.0",
  "private": true,
  "description": "Learner Wellbeing, Safety & Success Platform (P2-D05) — the authoritative domain for a learner's physical, emotional, behavioural, psychological and social wellbeing: wellbeing profile, health record, behaviour record, counselling and safeguarding cases, learner support and intervention plans; Student-linked, with fine-grained privacy authorization.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@knowget/shared": "workspace:*",
    "@knowget/types": "workspace:*",
    "@knowget/exceptions": "workspace:*",
    "@knowget/events": "workspace:*"
  },
  "devDependencies": {
    "@knowget/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
