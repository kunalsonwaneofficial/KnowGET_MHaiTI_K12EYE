{
  "name": "@knowget/assessment-evaluation",
  "version": "0.1.0",
  "private": true,
  "description": "Assessment, Evaluation & Competency Intelligence Platform (P2-D10) — the authoritative domain for planning, conducting, evaluating and reporting on learner assessment: assessment frameworks, plans, assessments, question banks, auditable evaluations, competency profiles and immutable academic records, with a pure grading engine and assessment-intelligence engine. Measures competency mastery independently of raw marks.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@knowget/shared": "workspace:*",
    "@knowget/types": "workspace:*",
    "@knowget/exceptions": "workspace:*",
    "@knowget/events": "workspace:*"
  },
  "devDependencies": {
    "@knowget/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
