{
  "name": "@knowget/authorization",
  "version": "0.1.0",
  "private": true,
  "description": "Authorization engine — RBAC with policy evaluation (ABAC-ready). No business roles.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@knowget/auth": "workspace:*",
    "@knowget/exceptions": "workspace:*"
  },
  "devDependencies": {
    "@knowget/config": "workspace:*",
    "@knowget/types": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
