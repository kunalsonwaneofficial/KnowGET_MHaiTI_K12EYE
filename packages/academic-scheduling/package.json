{
  "name": "@knowget/academic-scheduling",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise Academic Scheduling & Resource Orchestration Platform (P2-D07) — the authoritative scheduling engine for academic and institutional resource planning: timetables, schedule slots, schedulable resources, allocations, version-controlled scheduling policies, substitutions, and a pure conflict-detection / teacher-workload / scheduling-intelligence core.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@knowget/shared": "workspace:*",
    "@knowget/types": "workspace:*",
    "@knowget/exceptions": "workspace:*",
    "@knowget/events": "workspace:*"
  },
  "devDependencies": {
    "@knowget/config": "workspace:*",
    "eslint": "^9.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
