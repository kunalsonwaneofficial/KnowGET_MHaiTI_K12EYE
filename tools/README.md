# tools

Repository-level developer tooling (code generators, custom lint rules,
migration helpers) added as the platform grows. Empty at P1-M01 by design —
shared _runtime_ code belongs in `packages/`, and one-off automation belongs in
`scripts/`.
